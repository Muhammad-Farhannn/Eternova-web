{
  "name": "eternova-frontend",
  "version": "1.0.0",
  "description": "Eternova Static Frontend",
  "scripts": {
    "dev": "serve .",
    "build": "mkdir -p public && cp index.html public/"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
