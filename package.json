{
  "name": "eternova-frontend",
  "version": "1.0.0",
  "description": "Eternova Static Frontend",
  "scripts": {
    "dev": "serve .",
    "build": "mkdir -p public && cp index.html public/ && cp *.jpg public/ && cp -r mobile public/ 2>/dev/null || true"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
