{
  "name": "eternova-frontend",
  "version": "1.0.0",
  "description": "Eternova Static Frontend",
  "scripts": {
    "dev": "serve .",
    "build": "mkdir -p public && cp *.html public/ && cp *.jpg public/ && cp -r mobile public/ 2>/dev/null || true"
  },
  "dependencies": {
    "serve": "^14.2.0",
    "@supabase/supabase-js": "^2.38.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.5",
    "stripe": "^13.6.0"
  }
}
