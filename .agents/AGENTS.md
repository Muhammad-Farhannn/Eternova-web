# Workspace Rules

- Whenever you make any changes to the code, automatically deploy the changes to Vercel without asking for permission. You can do this by running `npx vercel --prod --yes --scope farhanhehunyawr-2966s-projects` in the project directory.
